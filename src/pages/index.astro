
---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Hero from '@/components/Hero.astro';
import { getStory } from '@/lib/cms/storyblok';

// const isDraft = Astro.url.searchParams.get('_draft') === '1';
// const story = await getStory('home', { draft: isDraft });
// Making sure draft works. Once this is in production and you're sure it's working, uncomment the two lines and delete the forced one
const story = await getStory('home', { draft: true });

const c = story?.content ?? {};
---
<BaseLayout title="DDP — Outdoor Structure Plans & Renderings">
  <Hero title={c.heroTitle} subtitle={c.heroSubtitle} ctaLabel={c.ctaLabel} ctaUrl={c.ctaUrl} />
</BaseLayout>

